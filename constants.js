// Canvas config
const WIDTH = 600;
const HEIGHT = 400;

// Bird config
const BIRD_RADIUS = 16;
const GRAVITY = 0.8;
const LIFT = -12;

// Pipes config
const PIPE_WIDTH = 80;
const PIPE_SPACING = 125;
const PIPE_SPEED = 6;
const FRAMES_PER_PIPE = 75;

// Genetic algorithm config
const POPULATION_SIZE = 500;
const MUTATION_RATE = 0.1;